# 2026-02-18

- 用户新增明确偏好：每次调用 `codex-cursor-playbook` skill 后，若有新的使用技巧和经验，需要：
  1) 同步更新到 skill 内容；
  2) 只有在用户明确允许后才 push 到远程 GitHub；
  3) 除更新 GitHub skill 外，也要同步更新到本地 memory。
- 本次会话已确认将按该流程执行。
- 新增 codex-cursor-playbook 实战经验（已同步到 skill 本地改动）：
  - `/speckit.implement` 优先拆 US1→US4 分步；
  - Free 计划优先 `--model auto`；
  - 明确运行文件白名单（避免生成旁路 `todo.*` 文件）；
  - 每步要求 changed files + verification evidence；
  - “无输出”先判进程存活，再决定等待/中断/拆步。
